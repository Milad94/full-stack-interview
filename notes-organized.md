# یادداشت‌های طراحی Sync

این سند خلاصهٔ تصمیم‌هایی است که برای Sync کردن فاکتورها و تراکنش‌ها از سرویس خارجی گرفتیم. هدفم این بوده که راه‌حل تا جای ممکن ساده بماند، ولی خطاهای شبکه، دادهٔ تکراری و اجرای ناقص هم باعث خراب شدن اطلاعات نشوند.

## تصویر کلی راه‌حل

یک اجرای Sync داریم که فاکتورها و تراکنش‌ها را از دو endpoint جدا می‌گیرد و داخل دیتابیس خودمان ذخیره می‌کند.

این دو منبع باید checkpoint مستقل داشته باشند. دلیلش این است که ممکن است Sync فاکتورها موفق شود ولی Sync تراکنش‌ها شکست بخورد. در این حالت فقط checkpoint فاکتورها جلو می‌رود و تراکنش‌ها در اجرای بعدی از checkpoint قبلی خودشان دوباره خوانده می‌شوند.

ذخیره‌سازی باید idempotent باشد؛ یعنی اگر یک صفحه یا یک رکورد دوباره دریافت شد، دادهٔ تکراری ساخته نشود. برای این کار رکوردها را با شناسهٔ خارجی‌شان Upsert می‌کنیم.

## محدودیت سرعت سرویس خارجی

محدودیت ۱۲۰ درخواست در دقیقه بین endpoint فاکتورها و تراکنش‌ها مشترک است. یعنی این‌طور نیست که برای invoices تعداد ۱۲۰ درخواست و برای transactions هم ۱۲۰ درخواست جدا داشته باشیم.

حداکثر `page_size` برابر ۱۰۰ است. پس سقف نظری دریافت داده می‌شود:

```text
۱۲۰ درخواست در دقیقه × ۱۰۰ رکورد = ۱۲٬۰۰۰ رکورد در دقیقه
```

در ۳۰ دقیقه، سقف کاملاً نظری حدود ۳۶۰ هزار رکورد است. مقدار واقعی کمتر می‌شود، چون خطای شبکه، retry، تأخیر سرویس و زمان ذخیره در دیتابیس را هم داریم.

این راه‌حل فقط وقتی پایدار می‌ماند که سرعت دریافت تغییرات از سرعت تولید آن‌ها در vendor بیشتر باشد. اگر vendor سریع‌تر از ظرفیت API داده تولید کند، عقب‌ماندگی Sync مدام بیشتر می‌شود و retry یا زیاد کردن فاصلهٔ اجرای job مشکل اصلی را حل نمی‌کند.

در آن شرایط باید خود قرارداد vendor بهتر شود؛ مثلاً:

- rate limit افزایش پیدا کند؛
- `page_size` بزرگ‌تر شود؛
- bulk export ارائه شود؛
- یا تغییرات از طریق webhook/change feed ارسال شوند.

## مشکل pagination فعلی

سرویس فعلی از شمارهٔ صفحه استفاده می‌کند و در پشت صحنه با offset جلو می‌رود. مشکل این روش این است که dataset هنگام گرفتن صفحات ثابت نیست.

فرض کنیم داده‌ها این‌طور صفحه‌بندی شده‌اند:

```text
A B C | D E F
```

صفحهٔ اول را می‌گیریم و `A`، `B` و `C` را ذخیره می‌کنیم. قبل از گرفتن صفحهٔ دوم، `A` در vendor آپدیت می‌شود و چون مرتب‌سازی بر اساس `updated_at` است، به انتهای لیست می‌رود:

```text
B C D | E F A
```

حالا اگر صفحهٔ دوم را بگیریم، `E`، `F` و `A` را می‌بینیم. `A` دوباره دریافت شده که با Upsert قابل‌حل است، ولی `D` کاملاً جا افتاده است.

### راه‌حل اصولی سمت vendor

راه بهتر این است که vendor از cursor pagination واقعی استفاده کند. در این روش به‌جای اینکه بگوییم «صفحهٔ دوم را بده»، می‌گوییم «از بعد آخرین موقعیتی که قبلاً دادی ادامه بده».

مثلاً cursor می‌تواند در عمل موقعیتی شبیه این را نشان دهد:

```text
(updated_at, id)
```

Cursor الزاماً رمزنگاری‌شده نیست. برای کلاینت فقط یک مقدار opaque است؛ یعنی نباید داخلش را تفسیر یا تغییر بدهیم و فقط همان را به vendor برگردانیم. ضمن اینکه صرف اسم `cursor` تضمینی ایجاد نمی‌کند؛ قرارداد API باید واقعاً ادامه از یک موقعیت پایدار را تضمین کند، نه اینکه offset را داخل یک رشته پنهان کرده باشد.

### کاری که با API فعلی می‌توانیم انجام بدهیم

با API فعلی راه‌حل صددرصدی نداریم، ولی می‌توانیم احتمال جاافتادن رکورد را کمتر کنیم:

1. `updated_since` را در تمام صفحات یک پیمایش ثابت نگه می‌داریم.
2. شناسه‌های یکتایی را که در همان پیمایش دیده‌ایم، نگه می‌داریم.
3. `total` پاسخ‌ها را بررسی می‌کنیم.
4. اگر تعداد IDهای یکتا کمتر از `total` بود یا `total` بین صفحات تغییر مشکوکی داشت، دوباره از صفحهٔ اول اسکن می‌کنیم.
5. تعداد اسکن مجدد محدود است؛ مثلاً حداکثر سه بار.
6. اگر بعد از آخرین تلاش هنوز ناسازگاری داشتیم، Sync آن منبع را failed می‌کنیم و checkpoint آن را جلو نمی‌بریم.

این IDها فقط برای تشخیص پوشش هستند و نباید باعث شوند پردازش رکورد تکراری را skip کنیم. ممکن است همان ID دوباره برسد ولی محتوایش تغییر کرده باشد؛ پس هر payload دریافتی باید Upsert شود.

همچنین `total` یک snapshot ثابت از ابتدای Sync نیست. این مقدار در هر درخواست با وضعیت همان لحظهٔ vendor محاسبه می‌شود. برابر شدن تعداد IDها با `total` می‌تواند بعضی جاافتادگی‌ها را پیدا کند، ولی تضمین نمی‌کند محتوای همهٔ رکوردها آخرین نسخه باشد.

یک محدودیت مهم دیگر هم Full Sync اولیه است. اگر یک رکورد قدیمی در Full Sync جا بیفتد، overlap کوتاه Syncهای بعدی آن را پیدا نمی‌کند؛ چون `updated_at` آن مربوط به مدت‌ها قبل است. اسکن محدود و دوبارهٔ Full Sync می‌تواند ریسک را کمتر کند، ولی تضمین کامل فقط با cursor یا snapshot درست از سمت vendor ممکن است.

## استقلال Sync فاکتورها و تراکنش‌ها

ممکن است endpoint فاکتورها خراب یا بسیار کند باشد ولی endpoint تراکنش‌ها سالم بماند. خرابی یا کندی یک منبع نباید بودجهٔ زمانی منبع دیگر را مصرف کند. محدود کردن تعداد retry به‌تنهایی کافی نیست؛ دریافت صفحات متعدد، پاسخ بسیار کند یا انتظار طولانی می‌تواند تمام زمان یک اجرای Sync را بگیرد.

برای همین از ابتدا برای هر منبع بودجهٔ زمانی مستقل و برابر کنار می‌گذاریم. با سقف پیش‌فرض ۴۰ دقیقه برای کل Sync، هر منبع حداکثر ۱۹ دقیقه و ۳۰ ثانیه فرصت دارد. ۳۰ ثانیه برای reconciliation و ثبت نتیجه، و ۳۰ ثانیه برای پایان‌بندی پس از soft timeout باقی می‌ماند. بودجهٔ هر منبع با تغییر سقف کلی دوباره محاسبه می‌شود و از شروع همان منبع حساب می‌شود، نه از شروع منبع قبلی. زمان استفاده‌نشدهٔ یک منبع به دیگری منتقل نمی‌شود.

بودجهٔ منبع شامل تمام صفحات، اسکن‌های مجدد، درخواست‌های HTTP، backoff، انتظار cooldown و ذخیرهٔ صفحات است؛ با هر صفحه یا retry از نو شروع نمی‌شود. برای قطع انتظار جاری، در worker لینوکسی prefork از تایمر `SIGALRM` استفاده می‌کنیم. فقط بررسی زمان بین دو صفحه کافی نیست، چون ممکن است درخواست فعلی هنوز تمام نشده باشد.

اگر invoices به سقف زمان خودش برسد یا retryهایش تمام شود، همان منبع failed می‌شود و transactions با بودجهٔ مستقل خودش اجرا می‌شود. همین قاعده برای شکست transactions برقرار است و نتیجه و checkpoint موفق invoices حفظ می‌شود. شکست هر منبع، نتیجهٔ کلی Run را failed می‌کند، حتی اگر دیگری موفق شده باشد.

هنگام timeout، تراکنش دیتابیسِ صفحهٔ در حال ذخیره rollback می‌شود؛ صفحات قبلاً ثبت‌شده باقی می‌مانند و checkpoint منبع ناقص جلو نمی‌رود. شمارنده‌ها را از دیتابیس دوباره می‌خوانیم تا تغییرات rollback‌شده به‌عنوان دادهٔ ذخیره‌شده گزارش نشوند. برای رفتن به منبع بعدی قفل کلی Sync آزاد نمی‌شود؛ reconciliation هم پس از پردازش هر دو منبع انجام می‌شود.

خرابی دیتابیس، از دست رفتن قفل، اشتباه بودن API key یا خرابی خود پردازش worker مشکلات سراسری هستند. در این موارد ادامه دادن منبع بعدی ممکن نیست یا ایمن نیست. سقف کلی Celery نیز همچنان محافظ نهایی اجرای غیرعادی است.

## تراکنشی که فاکتورش هنوز ذخیره نشده

README می‌گوید ممکن است یک تراکنش به فاکتوری اشاره کند که هنوز در دیتابیس محلی ما ذخیره نشده است. این جمله تضمین نمی‌کند که فاکتور در خود vendor وجود ندارد؛ فقط می‌دانیم هنوز آن را سمت خودمان نداریم.

برای همین در مدل Transaction دو فیلد جدا لازم داریم:

```text
external_invoice_id
invoice_fk
```

دو حالت متفاوت داریم:

```text
external_invoice_id = null
invoice_fk = null
```

یعنی vendor اصلاً مرجع فاکتور نداده است.

و:

```text
external_invoice_id = "INV-123"
invoice_fk = null
```

یعنی شناسهٔ فاکتور را داریم، ولی فاکتور محلی هنوز پیدا نشده است. اگر فقط FK nullable را ذخیره کنیم، در حالت دوم شناسهٔ لازم برای اتصال آینده را از دست می‌دهیم.

در پایان هر Sync یک reconciliation انجام می‌دهیم: تمام تراکنش‌های unresolved فعلی و قبلی را پیدا می‌کنیم و اگر Invoice متناظر حالا وجود داشت، FK را وصل می‌کنیم. فقط بررسی تراکنش‌های همین اجرا کافی نیست، چون ممکن است تراکنش قدیمی دیگر در نتیجهٔ Incremental Sync برنگردد.

باقی ماندن unresolvedها به‌تنهایی دلیل fail شدن Sync نیست، ولی تعدادشان را ثبت می‌کنیم تا اگر مدام زیاد شدند قابل‌بررسی باشد. تراکنش را حذف نمی‌کنیم و فاکتور جعلی هم نمی‌سازیم.

## Retry درخواست‌های vendor

سرویس خارجی ممکن است خطا بدهد، کند شود یا با `429` اعلام کند که فعلاً درخواست بیشتری نفرستیم.

برای هر درخواست timeout اتصال و خواندن پاسخ داریم. خطاهای موقت مثل timeout، قطع اتصال و `500/502/503` را با exponential backoff و کمی jitter دوباره امتحان می‌کنیم. خطاهایی مثل `401` و `422` با تکرار همان درخواست درست نمی‌شوند و retry نمی‌شوند.

تعداد تلاش‌ها محدود است؛ مثلاً حداکثر پنج attempt برای هر درخواست. بهتر است در مستند روشن کنیم که منظور پنج تلاش در مجموع است، نه یک تلاش اولیه به‌علاوهٔ پنج retry.

### رفتار با 429

اگر پاسخ این باشد:

```http
429 Too Many Requests
Retry-After: 18
```

حداقل ۱۸ ثانیه صبر می‌کنیم و بعد دقیقاً همان صفحه را دوباره درخواست می‌دهیم. نباید زودتر از `Retry-After` درخواست بفرستیم.

چون rate limit بین invoices و transactions مشترک است، cooldown هم باید مشترک باشد. یعنی اگر آخرین درخواست invoices با `Retry-After: 18` تمام شد، نباید بلافاصله سراغ transactions برویم؛ منبع بعدی هم باید تا پایان cooldown صبر کند.

timeout منبع، cooldown مشترک را پاک نمی‌کند. مثلاً اگر بعد از ۵ ثانیه از انتظار ۱۸ ثانیه‌ای، بودجهٔ invoices تمام شود، transactions باید ۱۳ ثانیهٔ باقی‌مانده را رعایت کند. اگر cooldown از بودجهٔ transactions هم بیشتر باشد، آن منبع نیز با خطای timeout تمام می‌شود و هیچ درخواست زودهنگامی نمی‌فرستد. این محدودیت مشترک vendor است؛ استقلال منابع اجازهٔ نادیده گرفتن آن را نمی‌دهد.

### چرا retry را در سطح request انجام می‌دهیم؟

برای این take-home، retry همان درخواست داخل task ساده‌تر است. worker در زمان backoff با `sleep` منتظر می‌ماند و بعد همان صفحه را دوباره درخواست می‌دهد.

اگر از `Celery retry(countdown=...)` استفاده کنیم، اجرای فعلی task تمام می‌شود و Celery بعداً تابع را دوباره از اول اجرا می‌کند. شمارهٔ صفحه، شمارنده‌ها و state داخل حافظه خودکار منتقل نمی‌شوند و باید آن‌ها را در دیتابیس checkpoint کنیم.

از طرف دیگر advisory lock ما به session دیتابیس وابسته است. وقتی اجرای فعلی تمام شود و connection بسته شود، قفل آزاد می‌شود. اجرای retry شده با session جدیدی می‌آید و نمی‌تواند همان قفل قبلی را با خودش ادامه بدهد. حل کامل این موضوع به lease یا قفل پایدار، heartbeat و recovery از crash نیاز دارد که برای اندازهٔ این تمرین پیچیدگی اضافه است.

پس تصمیم فعلی این است:

```text
request-level retry + sleep محدود
```

اگر بعداً مدت انتظارها زیاد شد و اشغال worker واقعاً مشکل ایجاد کرد، می‌توانیم state هر مرحله و یک lease پایدار را اضافه کنیم و retry را به Celery بسپاریم.

## جلوگیری از اجرای هم‌زمان

برای اینکه دو Sync روی هم اجرا نشوند، قبل از شروع advisory lock پستگرس می‌گیریم. این قفل به connection دیتابیس وابسته است و با بسته شدن connection یا crash شدن worker خودکار آزاد می‌شود.

اگر task جدید نتواند قفل را بگیرد، یعنی یک Sync دیگر فعال است و اجرای جدید باید skip شود. اگر قفل را گرفت، می‌تواند کار خودش را شروع کند.

آزاد شدن قفل بعد از crash، وضعیت `SyncRun` را خودکار اصلاح نمی‌کند. ممکن است رکورد اجرای قبلی همچنان `running` مانده باشد. اجرای جدید فقط بعد از گرفتن قفل می‌تواند اجرای قبلی را `interrupted` کند؛ قدیمی بودن زمان شروع به‌تنهایی کافی نیست، چون شاید اجرای قبلی واقعاً هنوز فعال باشد.

## سقف زمانی کل Sync

هر Sync باید سقف زمانی قابل‌تنظیم داشته باشد تا یک اجرای غیرعادی، worker و lock را برای مدت نامحدود نگه ندارد. این خواسته بخشی از همین پیاده‌سازی است. مقدار پیش‌فرض ۴۰ دقیقه است و با `ACCOUNTING_SYNC_TIME_LIMIT_SECONDS` تغییر می‌کند؛ زمان ماندن در صف جزو آن نیست.

در حالت پیش‌فرض، Celery در ۳۹ دقیقه و ۳۰ ثانیه soft timeout می‌دهد تا شکست ثبت و قفل آزاد شود. اگر خود پایان‌بندی هم گیر کرده باشد، در سقف ۴۰ دقیقه پردازش child را متوقف می‌کند. با بسته‌شدن session دیتابیس، قفل آزاد می‌شود و اجرای بعدی بعد از گرفتن قفل، رکورد running رهاشده را interrupted می‌کند. بودجه‌های مستقل منابع پیش از این سقف‌ها تمام می‌شوند تا timeout معمول یک endpoint به منبع دیگر لطمه نزند.

تایمر منبع و soft timeout به اجرای handler پایتون وابسته‌اند؛ اگر خود runtime یا یک عملیات native به signal پاسخ ندهد، سقف سخت Celery محافظ نهایی است. ادامهٔ همان Run پس از کشته‌شدن پردازش ممکن نیست. این رفتار به worker لینوکسی با pool نوع prefork متکی است؛ اجرای مستقیم تابع یا poolهای دیگر جایگزین تضمین سقف سخت Celery نیست.

عدد ۴۰ دقیقه پیش‌فرض عملیاتی قابل‌تغییر است و می‌تواند بعد از اندازه‌گیری اصلاح شود. این deadline مشکل vendor یا کمبود ظرفیت را حل نمی‌کند؛ فقط اثر یک اجرای غیرعادی را محدود می‌کند.

اگر مشکل vendor موقتی باشد، اجرای دستی یا نوبت بعدی از checkpoint قبلی دوباره تلاش می‌کند. اگر مشکل ساختاری باشد، مثلاً سرعت تولید داده از ظرفیت دریافت بیشتر باشد، اجرای بعدی هم شکست می‌خورد و باید قرارداد یا ظرفیت سرویس خارجی اصلاح شود.

## خلاصهٔ تصمیم‌ها

- rate limit بین هر دو endpoint مشترک است.
- invoices و transactions checkpoint مستقل دارند.
- هر منبع بودجهٔ زمانی مستقل دارد؛ timeout یک endpoint مانع اجرای منبع دیگر نمی‌شود، مگر محدودیت یا خرابی سراسری.
- هر payload با شناسهٔ خارجی Upsert می‌شود و تکرار قابل‌تحمل است.
- `updated_since` در تمام صفحات یک پیمایش ثابت می‌ماند.
- بررسی `total` و IDهای یکتا فقط یک راه تشخیص کمکی است، نه تضمین کامل.
- اسکن مجدد، retry و زمان انتظار همگی محدود هستند.
- خطاهای موقت retry می‌شوند و `Retry-After` به‌طور کامل رعایت می‌شود.
- cooldown مربوط به 429 بین هر دو endpoint مشترک است.
- تراکنش unresolved با `external_invoice_id` نگه داشته و بعداً reconcile می‌شود.
- advisory lock جلوی اجرای هم‌زمان را می‌گیرد.
- checkpoint منبع ناموفق جلو نمی‌رود.
- سقف قابل‌تنظیم کل Sync الزامی است؛ پیش‌فرض آن ۴۰ دقیقه است و جای بودجه‌های مستقل منابع را نمی‌گیرد.
