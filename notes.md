- یه مشکل یا باگی که توی سرویس خارجی هست اینه که تاریخ updated_at رو داره در اینده میده! یعنی مقدار occured_at رو در اینده میده و updated _at رو هم مساوی همون میذاره. به نظر میاد اینجا اشتباه پیاده سازی بوده. اما برای اینکه منطق اپلکیشن من حتی با وجود این خطا درست کنار بیاد من مقدار watermark رو برابر آخرین زمان اجرای sync گذاشتم. با اینکار درسته که داده تکراری پردازش میشه ولی خطر از دست دادن داده دیگه نیست.چون ممکنه بعد از رکوردی که updated at اش واسه مثلا ۳ روز دیگه اس رکوردی ایجاد بشه که مال امروزه و وقتی ما واترمارک رو روی ۳ روز آینده بذاریم دیگه اون رکورد امروز از دست میره!

# مشکل دقت Updated_at

- سرویس خارجی دقت زمانش در حد ثانیه هست. یعنی اگر توی یک ثانیه(با میلی ثانیه های متفاوت) دو تا تغییر روی یک فاکتوری اتفاق افتاده باشه و ما بررسی تغییر رکورد رو روی زمان updated_at گذاشته باشیم نمیفهمیم تغییر اتفاق افتاده. باید همه فیلدا یک به یک مقایسه بشن

## مثال ساده

فرض کن این فاکتور در دیتابیس ما وجود دارد:

```
status: open
amount_paid: 100
updated_at: 10:00:00
```

در همان ثانیه دوباره تغییر می‌کند و vendor این نسخه را می‌دهد:

```
status: paid
amount_paid: 200
updated_at: 10:00:00
```

محتوا عوض شده، ولی `updated_at` ثابت مانده است.

اگر منطق ما این باشد:

```
if incoming.updated_at <= stored.updated_at:
    ignore()
```

رکورد جدید نادیده گرفته می‌شود، چون:

```
10:00:00 <= 10:00:00
```

در نتیجه فاکتور ما اشتباهاً همچنان `open` و مبلغش `100` باقی می‌ماند.

## Upsert درست

شناسهٔ خارجی را مبنای پیدا کردن رکورد قرار می‌دهیم:

```
external_id = INV-123
```

سپس محتوای فیلدها را بررسی می‌کنیم:

```
رکورد وجود ندارد         → ایجادش کن
رکورد هست و محتوا فرق دارد → به‌روزرسانی کن
رکورد هست و محتوا یکی است  → هیچ کاری نکن
```













- سرویس خارجی محدودیت ۱۲۰ تا رو مشترک بین اندپوینت ها گذاشته و اینجور نیست اندپوینت فاکتور ۱۲۰ تا و اندپوینت  تراکنش ۱۲۰ تا باشه





# راه حل زمانی استیبل هست که سرعت دریافت تغییرات سمت ما بیشتر از تولید اونها سمت سرویس خارجی باشه





سرویس خارجی محدودیت ۱۲۰ تا درخواست در دقیقه رو داره. پس حداکثر ۱۲۰*۱۰۰ که میشه ۱۲۰۰۰ رکورد در دقیقه میتونیم ازش سینک کنیم و در بهترین حالت توی یک سینک ۳۰ دقیقه ای حدود ۳۶۰ هزار تا تغییر رو بگیریم(در حالت نظری) و احتمالا مقدار واقعی کمتر از ۳۰۰ هزار هست به خطای تاخیر شبکه و دیتابیس و ...





پس اگه بیشتر از ۳۰۰  هزار تا رکورد جدید سمت سرویس خارجی ایجاد شده باشه محدودیت ۱۲۰ تا درخواست مشکل سازه





- پس یا باید سرویس خارجی محدودیت ۱۲۰ تا درخواست رو بیشتر کنه
- یا page size رو بزرگتر کنه
- یا bulk export بده بهمون
- یا با وب هوک تغییرات رو آنی به ما اعلام کنه





# مشکلات سرویس خارجی و تصمیم طراحی

# ۱ 

سرویس خارجی داره از روش page offset استفاده میکنه. این باعث میشه وقتی یه رکوردی سمت خودش ابدیت میشه بیفته ته لیست و دوبار پردازش بشه یا یه رکوردی جا بیفته.

راه حل این مشکل اینه که به جای روش page offset از روش cursor استفاده بشه که اون مشکل جا اتفتادنه حل بشه



یعنی اینو فرض کنیم 

```
A B C | D E F
```



فرض کنیم ما پیج اول رو میگیرم و توی هر پیج سه تا هست

پس ما A و B و C رو گرفتیم. حالا که میخوایم پیج دوم رو بگیریم توی این فاصله یه ابدیتی سمت سرویس خارجی اتفاق میفته و A ابدیت میشه و میره ته لیست. در نتیجه این میشه 

```
B C D | E F A
```

اینجا D کلا جا میفته و A دوبار پردازش میشه



توی روش Cursor سرویس خارجی به ما یه رشته ای میده که وقتی اونو به خودش دادیم میبینه تا کجا رو قبلا به ما داده . مثلا توی اون رشته که رمزنگاری شده هم هست میگه من قبلا تا C رو به کلاینت دادم. خب حالا پس D E F رو بهش میدم و حالا کرسر جدید میشه F حالا وقتی اجرا میکنه A رو میگیره که تکراری هست و این سمت ما قابل حله با idempotent بودن ولی دیگه مشکل جا افتادن D رو نداریم





با API‌ فعلی راه حل صد در صدی براش وجود نداره. اما یه چیزی هست که میتونه کمک کننده باشه و اثرش رو کمتر کنه



چون اندپوینت ها total رو دارن به ما میدن میتونیم رکوردهای یکتایی که گرفتیم رو در انتهای گرفتن صفحات بشماریم و  اگه کمتر از total بود دوباره از page اول اسکن رو شروع کنیم و دو یا نهایتا ۳ بار کل اسکن رو تکرار کنیم تا همه رکوردها گرفته بشن. اما اگه اینقدر لیست سریع تغییر کنه که با این ۲ یا ۳ تا اسکن هم نشه همه رکوردا رو گرفت دیگه کاریش نمیشه کرد و ما نمیتونیم سمت خودمون بی نهایت بار تلاش کنیم. در نتیجه توی این حالت وقتی مثلا برای بار سوم هم مشکل رخ داد اون run رو fail میکنیم و علت رو ذکر میکنیم. در نتیجه دیتایی که ثبت میشه به ما این دید رو میده که اگه دیدیم مدام داره  fail اتفاق میفته به تیم سرویس خارجی بگیم روششون رو عوض کنن ولی اگه دیدیم نه نرخ تغییرات قابل مدیریته با همین روش هم میشه مشکل رو حل کرد. یعنی موقع کار کردن سیستم بر اساس رفتارش تصمیم فنی میگیریم.



نکته مهم اینه چون دقت updated_at توی سرویس خارجی روی ثانیه هست پس اگه یه ابدیتی اتفاق بیفته روی یه رکوردی که خیلی سریع باشه این روش total ما تشخیصش نمیده چون رکورد توی مرتب سازی جا به جا نمیشه.



بعد از گرفتن هر صفحه ای باید اون total اخرین صفحه بروز بشه. چون ممکنه اون وسط دیتای جدیدی اضافه شده باشه





# ۲

اندپوینت فاکتور ممکنه به هر دلیلی خراب باشه ولی اندپوینت تراکنش ها سالم باشه. هیچکدوم نباید مانع سینک اونیکی بشن. پس باید تایم اوت و خطاهای سرویس و retry محدود بشه که اجرای یکی مانع اونیکی نشه حتی اگه پشت سر هم صدا زده بشن 





# ۳

بخصوص که توی توضیحات تسک گفته شده ممکنه تراکنشی ایجاد بشه که هنوز فاکتورش حتی توی سمت سرویس خارجی وجود نداره. این ممکنه به دلایل مختلف سمت سرویس خارجی اتفاق بیفته ولی در نهایت به eventual consistency برسیم. 



پس باید یه راهی داشته باشیم که reconcilation انجام بدیم و در انتهای هر سینک بررسی کنیم اگه میشه ارتباط فاکتور و تراکنش رو برقرار کنیم(نیاز به جاب اضافه نداره)



# ۴

سرویس خارجی ممکنه خطای ۴۲۹ بده و بگه بعد از مثلا ۱۸ ثانیه تلاش مجدد بکن و ریت لیمیت بخوریم.  اینجا ما باید حداقل ۱۸ ثانیه صبر کنیم. پس من روی همون اندپوینت ۱۸.۵ ثانیه صبر میکنم و دقیقا همون پیجی که بودم (نه از اول) رو درخواست میدم. اینکار رو هم به دفعات محدود انجام میدم (حداکثر مثلا ۵ بار) و اگر بازم به لیمیت خوردم سینک رو fail میکنم و دوباره تلاش مجدد نمیکنم (تعداد دفعات تلاش مجدد رو هم ثبت میکنم). اینجا من از retry توی سطح ریکوست استفاده میکنم و داخل همون کد sleep میکنم. نه retry سلری چون وقتی از روی retry سلری استفاده میکنیم سلری کل تسک رو از اول شروع میکنه و مثلا از ۱۸ ثانیه دیگه دوباره از پیج اول شروع به تلاش مجدد میکنه و کل کد از خط اولش اجرا میشه! برای حل این مشکل باید وضعیت رو دوباره بهش پاس بدیم که این پیچیده اس



مشکل بدترش اینه که قفلی که موقع اجرای تسک میگیریم باید موقع ریترای بگیم قفل آزاد بشه چون از advisory lock پستگرس استفاده میکنیم کانکشن بسته میشه و قفل آزاد میشه و توی اجرای بعدی که سلری انجام میده سشن دیگه ای از دیتابیس میاد و قفل رو میگیره. پس توی اون ۱۸ ثانیه چون قفل آزاده یه تسک دیگه میتونه بیاد قفل رو بگیره و اینجوری داده خراب نمیشه ولی لایف سایکل یک سینک خیلی پیچیده میشه



برای اینکه قفل بین retry های سلری منتقل بشه باید قفل دیگه این استفاده کنیم که وابسته به کانکشن نباشه. مثلا بریم یه رکورد توی دیتابیس ایجاد کنیم. اما دلیل انتخابم اینه که همیشه سیستم رو با ساده ترین حالت شروع کنیم و پیچیدگی فقط وقتی که لازمه اضافه بشه.



استفاده از sleep خیلی طراحی ساده تریه.

# ۵

# اگر ورکر سلری کرش کنه چی میشه؟

دیتای فاکتور و تراکنش و شمارنده ها رو بصورت تراکنشی ذخیره میکنیم و اگه وسطش برق سرور بره از این بابت مشکلی نیست و قفل هم ازاد میشه



اما خود آبجکت syncRun که وضعیت تسک رو توی دیتابیس داره ممکنه با کرش کردن سرور حتی فرصت تغیر به وضعیت fail رو پیدا نکنه و توی running بمونه



در نتیجه وقتی سرور بالا اومد و تسک سینک بعدی اجرا شد و تونست قفل رو بگیره با خیال راحت میتونه ببینه هر تسکی که runnign هست رو fail کنه و بعد به ادامه کارش برسه.



```
10:00:00 → SyncRun #42 شروع شد
10:00:01 → advisory lock گرفته شد
10:00:10 → Invoice page 1 commit شد
10:00:20 → worker وسط page 2 crash کرد

نتیجهٔ فوری:
- قفل خودکار آزاد می‌شود
- page 1 باقی می‌ماند
- page 2 rollback می‌شود
- watermark قبلی باقی می‌ماند
- SyncRun #42 ممکن است running بماند

10:30:00 → SyncRun #43 شروع می‌شود
10:30:01 → lock را می‌گیرد
10:30:02 → SyncRun #42 را failed می‌کند
10:30:03 → از watermark قبلی دوباره شروع می‌کند
10:30:10 → page 1 دوباره می‌آید و Upsert آن را no-op می‌کند
10:30:20 → page 2 با موفقیت ذخیره می‌شود
```



اما اگه خود سلری تسک رو انجام شده در نظر نگیره و دوباره به ورکر تحویلش بده چی؟  



ورکر میاد میبینه همون sync run از قبل توی دیتابیس وجود داره و توی وضعیت running هست. در نتیجه کارو ادامه میده و sync run جدیدی نمیسازه و در نهایت کارش تموم شد وضعیت این syncRun رو میکنه موفقیت آمیز







# ۶- جلوگیری از اجرای همزمان دو sync run که هر جفتشون بخوان برن سراغ دیتابیس 



این خواسته تسک هست و با pg advisory lock پیاده میشه



از redis lock استفاده نمیکنم چون دپندنسی جدید به پروژه اضافه میکنه





#  idempotent بودن

دو تا watermark روی هر syncrun داریم که هر کدوم میگه که توی اون syncrun تا کجا() جلو رفته اون ریسورس(فاکتور / تراکنش). فقط وقتی واتر مارک جلو میره که ...



آیدی خارجی هر ریسورس هم توی دیتابیس ما یونیک میشه که از درج تکراری جلوگیری بشه(اخرین سد دفاعی)



همیشه هر رکوردی رو upsert میکنیم. یعنی اگر وجود داشت ابدیتش میکنیم (اگر عوض شده بود) و در غیر اینصورت ایجادش میکنیم و اگرم هیچ تغییری نکرده بود pass میشه و هیچ کاری نمیکنیم





اگر updated_at که میاد برای یه رکورد از مقدار فعلی اش قدیمی تر باشه نادیده میگیریمش. اطلاعات هر صفحه ای که میگیریم(هر صفحه ۱۰۰ تا ریسورس داخلشه) به همراه شمارنده هایی که باید ابدیت بشه روی syncrun همشون توی یه تراکنش ذخیره میکنیم. در نتیجه حالتی وجود نداره که نصف رکوردهای یه صفحه ذخیره بشه نصفش نه





برای حل مشکل گم نشدن صفحه که ناشی از مشکل روش paginaion بود واترمارک رو فقط بعد از پایان کامل (زمانی که next page برابر null باشه!)

یعنی اگر توی یه سینک ران به ته تغییرات نرسیم سیستم همیشه از اول شروع به پردازش میکنه!



در واقع هدف این بوده هیچ صفحه ای جا نمونه و بخاطر همین سیستم متوقف میشه و مدام از اول شروع ممکنه بکنه.



این نشون دهنده دو حالت هست. یا تعداد رکوردهایی که قراره به ما برسه خیلی زیاده یا مدام داره ابدیت انجام میشه که در هر دو صورت نیازمندی اقدام از سمت سرویس خارجی برای تغییر api اش هستیم



watermark فقط بعد از اجرای تمام صفحات یک ریسورس داخل یک sync run میره جلو





# خطا دادن سرویس خارجی

اگه مشکل موقتی باشه(خطاهای رنج ۵۰۰)

همون صفحه رو دوباره درخواست میدیم retry و این retry هم با exponetioal backoff هست. به تعداد محدود مثلا ۳ بار تلاش میکنیم

ذخیره هر صفحه داخل یک تراکنش انجام میشه نه کل صفحات چون تراکنش طولانی باز میمونه

اگر مشکل رنج ۴۰۰ باشه(به جز 429 که ریت لیمیت هست) باید همون لحظه sync Run متوقف بشه و خطا ثبت بشه







# رفتار اجرای دستی از داشبورد

endpoint اجرای دستی نباید منتظر پایان sync بماند.

رفتار مناسب:

1. یک `SyncRun` با وضعیت `queued` بسازد.
2. task را در Celery enqueue کند.
3. سریعاً پاسخ `202 Accepted` برگرداند.
4. شناسهٔ `SyncRun` را به frontend بدهد.

مثلاً:

```
{
  "id": 42,
  "status": "queued"
}
```

فرانت‌اند بعداً این endpoint را poll می‌کند:

```
GET /api/sync-runs/42/
```

و نتیجه به‌ترتیب تغییر می‌کند:

```
queued → running → succeeded
```

یا:

```
queued → running → failed
```

------

## 







# چه تست هایی بنویسم؟

حداقل تست‌های الزام‌شده:

### اجرای دوم هیچ تغییری ایجاد نکند

```
اجرای اول → 240 invoice ساخته شد
اجرای دوم → 0 ساخته و 0 ویرایش شد
تعداد رکوردها همچنان 240 است
```

### شکست وسط pagination

```
صفحهٔ ۱ → 200
صفحهٔ ۲ → چند بار 503
```

باید بررسی شود:

- run وضعیت `failed` بگیرد
- داده‌های صفحهٔ اول سالم باشند
- watermark جلو نرفته باشد
- اجرای بعدی بتواند کامل شود
- duplicate ساخته نشود

تست‌های باارزش بعدی:

- پاسخ 429 و رعایت `Retry-After`
- timeout و retry
- چند رکورد با `updated_at` یکسان روی مرز watermark
- اجرای هم‌زمان دو task
- transaction بدون invoice
- اتصال transaction بعد از رسیدن invoice
- حفظ دقت مبالغ اعشاری
- fail شدن یک صفحه هنگام عملیات دیتابیس
- پاسخ ناقص یا JSON نامعتبر vendor













## نمونهٔ endpoint اجرای دستی

منطقش تقریباً این است:

```
run = SyncRun.objects.create(
    trigger=SyncRun.Trigger.MANUAL,
    status=SyncRun.Status.QUEUED,
)

sync_external_data.delay(run.id)

return Response(
    {
        "id": run.id,
        "status": run.status,
        "queued_at": run.queued_at,
    },
    status=202,
)
```

خود Celery task، `run_id` را دریافت می‌کند:

```
@shared_task
def sync_external_data(run_id):
    run = SyncRun.objects.get(id=run_id)

    run.status = SyncRun.Status.RUNNING
    run.started_at = timezone.now()
    run.save()
```

## اجرای خودکار چه می‌شود؟

برای اینکه `queued_at` در اجرای دستی و خودکار معنی یکسانی داشته باشد، بهتر است یک تابع مشترک داشته باشیم:

```
def enqueue_sync(trigger):
    run = SyncRun.objects.create(
        trigger=trigger,
        status=SyncRun.Status.QUEUED,
    )

    sync_external_data.delay(run.id)
    return run
```

اجرای دستی:

```
enqueue_sync(trigger="manual")
```

اجرای زمان‌بندی‌شده:

```
enqueue_sync(trigger="scheduled")
```

Celery Beat می‌تواند هر ۳۰ دقیقه یک task سبک مثل `enqueue_scheduled_sync` را اجرا کند:

```
@shared_task
def enqueue_scheduled_sync():
    enqueue_sync(trigger="scheduled")
```

در نتیجه فلوی خودکار می‌شود:

```
Celery Beat
    │ هر ۳۰ دقیقه
    ▼
enqueue_scheduled_sync
    │
    ▼
ساخت SyncRun با queued_at
    │
    ▼
قرار دادن sync_external_data در صف
```

## اگر Celery task را نپذیرد چه می‌شود؟(یعنی کد ما نتونه تسک رو بده به ربیت و توی صف ربیت نره)

ممکن است رکورد ساخته شود ولی ارسال پیام به RabbitMQ شکست بخورد. پس باید خطای enqueue را مدیریت کنیم:

```
ساخت SyncRun
      │
      ▼
ارسال به RabbitMQ شکست خورد
      │
      ▼
status = failed
error_message = "Could not enqueue task"
finished_at = اکنون
```

بهتر است نام وضعیت دقیق‌تری مثل `enqueue_failed` نسازیم؛ برای این تمرین همان `failed` همراه با پیام خطای واضح کافی است.(من فکر میکنم وضعیت enqueue_failed بهتره ) 

## اگر یک sync دیگر در حال اجرا باشد

رکورد جدید همچنان ایجاد شده و `queued_at` دارد. وقتی worker آن را اجرا می‌کند، نمی‌تواند lock بگیرد:

```
status      = skipped
finished_at = اکنون
error_message = "Another sync is already running"
```

خلاصهٔ معنی زمان‌ها:

```
queued_at   → چه زمانی sync درخواست و وارد صف شد؟
started_at  → چه زمانی worker واقعاً اجرای آن را شروع کرد؟
finished_at → چه زمانی اجرا تمام شد؟
```

نام `requested_at` شاید از نظر معنایی کمی دقیق‌تر از `queued_at` باشد، چون رکورد درست پیش از ارسال پیام ساخته می‌شود؛ اما `queued_at` نیز با این flow کاملاً قابل دفاع است.











منظور README از «queryهای مقیاس‌پذیر» این است:

> کدی بنویس که با ۲۴۰ رکورد خوب کار کند و اگر داده‌ها به صد هزار یا چند میلیون رکورد رسیدند، ناگهان بسیار کند یا پرمصرف نشود.

## مثال اول: جمع‌زدن مبلغ‌ها

روش نامناسب:

```
total = Decimal("0")

for invoice in Invoice.objects.all():
    total += invoice.total
```

در این روش تمام فاکتورها از دیتابیس خوانده، وارد حافظهٔ Python و سپس جمع زده می‌شوند.

اگر یک میلیون فاکتور داشته باشیم، یک میلیون رکورد منتقل می‌شود.

روش مقیاس‌پذیر:

```
from django.db.models import Sum

Invoice.objects.aggregate(total=Sum("total"))
```

دیتابیس خودش جمع را حساب می‌کند و فقط نتیجه را برمی‌گرداند:

```
{
  "total": "2845000.00"
}
```

## مثال دوم: محاسبهٔ مبلغ outstanding

روش نامناسب:

```
outstanding = Decimal("0")

for invoice in Invoice.objects.filter(status="open"):
    outstanding += invoice.total - invoice.amount_paid
```

روش مناسب:

```
from django.db.models import F, Sum

Invoice.objects.filter(status="open").aggregate(
    outstanding=Sum(F("total") - F("amount_paid"))
)
```

محاسبه کاملاً در PostgreSQL انجام می‌شود.

در این پروژه باید مبلغ هر currency جدا محاسبه شود:

```
(
    Invoice.objects
    .filter(status="open")
    .values("currency")
    .annotate(
        outstanding=Sum(F("total") - F("amount_paid"))
    )
)
```

نتیجه:

```
[
  {"currency": "USD", "outstanding": "1200.00"},
  {"currency": "EUR", "outstanding": "700.00"},
  {"currency": "GBP", "outstanding": "300.00"}
]
```

## مثال سوم: نمودار وضعیت فاکتورها

نباید تمام فاکتورها را بگیریم و در Python بشماریم:

```
for invoice in Invoice.objects.all():
    counts[invoice.status] += 1
```

بهتر است دیتابیس آن‌ها را گروه‌بندی کند:

```
from django.db.models import Count

Invoice.objects.values("status").annotate(
    count=Count("id")
)
```

نتیجه:

```
[
  {"status": "open", "count": 120},
  {"status": "paid", "count": 90},
  {"status": "draft", "count": 20},
  {"status": "void", "count": 10}
]
```

## مثال چهارم: مبلغ وصول‌شده در ماه جاری

روش مناسب:

```
(
    Transaction.objects
    .filter(
        type="payment",
        occurred_at__gte=start_of_month,
        occurred_at__lt=start_of_next_month,
    )
    .values("currency")
    .annotate(amount=Sum("amount"))
)
```

این query:

- فقط `payment`ها را در نظر می‌گیرد.
- فقط تراکنش‌های ماه جاری را انتخاب می‌کند.
- مبالغ را بر اساس currency جدا می‌کند.
- جمع را در دیتابیس انجام می‌دهد.

## مثال پنجم: Pagination

برای جدول adjustmentها نباید تمام رکوردها را یک‌جا برگردانیم:

```
Adjustment.objects.all()
```

endpoint باید صفحه‌بندی داشته باشد:

```
GET /api/adjustments/?page=2&page_size=25
```

یعنی در هر درخواست فقط تعداد محدودی رکورد خوانده و ارسال شود.

## مثال ششم: جلوگیری از N+1 Query

فرض کن ۲۵ adjustment نمایش می‌دهیم و برای هرکدام نام فاکتور را هم لازم داریم.

روش نامناسب ممکن است این رفتار را ایجاد کند:

```
۱ query برای adjustmentها
۲۵ query جدا برای invoice هر adjustment
```

یعنی در مجموع ۲۶ query.

روش مناسب:

```
Adjustment.objects.select_related("invoice")
```

حالا adjustment و invoice مرتبط با یک query خوانده می‌شوند.

## مثال هفتم: Index

اگر مرتب این query را اجرا کنیم:

```
Transaction.objects.filter(
    type="payment",
    occurred_at__gte=start_of_month,
)
```

وجود index زیر به PostgreSQL کمک می‌کند سریع‌تر رکوردها را پیدا کند:

```
models.Index(fields=["type", "occurred_at"])
```

بدون index، در حجم بالا ممکن است دیتابیس مجبور شود تمام جدول را بررسی کند.





## مثال هشتم: ذخیرهٔ داده‌های Sync

اگر هر صفحه ۱۰۰ فاکتور داشته باشد، انجام ۱۰۰ query جداگانه همیشه بهترین روش نیست:

```
SELECT invoice 1
UPDATE invoice 1

SELECT invoice 2
UPDATE invoice 2

...
```

در صورت امکان می‌توان رکوردهای موجود را یک‌جا خواند و تغییرات را گروهی ذخیره کرد:

```
Invoice.objects.bulk_create(new_invoices)
Invoice.objects.bulk_update(changed_invoices, fields=[...])
```

البته برای حجم کوچک این پروژه، یک پیاده‌سازی واضح و صحیح از بهینه‌سازی پیچیده مهم‌تر است. ولی نباید به ازای هر رکورد چند query غیرضروری ایجاد شود.

خلاصه، query مقیاس‌پذیر در این پروژه یعنی:

```
جمع و گروه‌بندی → داخل PostgreSQL
لیست‌های بزرگ → با pagination
روابط → با select_related
جست‌وجوی پرتکرار → با index مناسب
ذخیره‌سازی گروهی → در صورت نیاز با bulk operations
```









از چه قفلی پس باید استفاده کرد؟

Tuesday 8:38 PM

Worked for 49s

برای این پروژه بهترین انتخاب، **PostgreSQL advisory lock از نوع session-level** است:

```
pg_try_advisory_lock
```

دلیل انتخاب:

- PostgreSQL از قبل در پروژه وجود دارد.
- بین تمام processها و workerها مشترک است.
- dependency جدیدی مثل Redis لازم ندارد.
- اگر worker از بین برود و اتصال دیتابیس بسته شود، قفل خودکار آزاد می‌شود.
- با transactionهای کوتاهِ ذخیره‌سازی هر صفحه سازگار است.

## فلوی اجرا

```
Task اول
  │
  ├─ قفل را می‌گیرد → Sync اجرا می‌شود
  │
Task دوم
  │
  └─ قفل را نمی‌گیرد → وضعیت skipped
```

پیاده‌سازی پایه:

```
from django.db import connection

SYNC_LOCK_ID = 912_026


def try_acquire_sync_lock() -> bool:
    with connection.cursor() as cursor:
        cursor.execute(
            "SELECT pg_try_advisory_lock(%s)",
            [SYNC_LOCK_ID],
        )
        return cursor.fetchone()[0]


def release_sync_lock() -> None:
    with connection.cursor() as cursor:
        cursor.execute(
            "SELECT pg_advisory_unlock(%s)",
            [SYNC_LOCK_ID],
        )
```

در Celery task:

```
@shared_task
def sync_external_data(run_id: int):
    acquired = False

    try:
        acquired = try_acquire_sync_lock()

        if not acquired:
            mark_sync_as_skipped(run_id)
            return

        run_sync(run_id)

    finally:
        if acquired:
            release_sync_lock()
```

استفاده از `finally` مهم است تا در صورت خطا نیز قفل آزاد شود.





## چرا session-level؟

PostgreSQL دو مدل مهم advisory lock دارد:

```
pg_try_advisory_lock      → تا زمان unlock یا بسته‌شدن connection
pg_try_advisory_xact_lock → فقط تا پایان transaction
```

ما هر صفحه را در یک transaction کوتاه ذخیره می‌کنیم. اگر از قفل transaction-level استفاده کنیم، بعد از ذخیرهٔ صفحهٔ اول قفل آزاد می‌شود و sync دوم می‌تواند هم‌زمان شروع شود.

بنابراین باید از این استفاده کنیم:

```
pg_try_advisory_lock
```

نه از این:

```
pg_try_advisory_xact_lock
```

همچنین نباید یک transaction دیتابیس را در تمام مدت درخواست‌های HTTP باز نگه داریم؛ چون sync ممکن است به‌دلیل retry و `Retry-After` طولانی شود.

## چرا گزینه‌های دیگر مناسب نیستند؟

- `threading.Lock`: فقط داخل یک process کار می‌کند.
- قفل حافظه‌ای Django: بین workerها مشترک نیست.
- `select_for_update`: نیازمند باز نگه داشتن transaction در تمام مدت sync است.
- Redis lock: Redis در پروژه وجود ندارد و افزودنش اضافه‌کاری است.
- `worker_concurrency=1`: فقط worker فعلی را محدود می‌کند و در صورت اضافه شدن worker دوم دیگر تضمینی ندارد.

نکتهٔ مهم: برای `SYNC_LOCK_ID` از یک عدد ثابت استفاده کن، نه `hash("accounting-sync")`؛ چون خروجی `hash()` پایتون ممکن است بین processها متفاوت باشد.







## ۸. شکست در وسط Pagination

فرض کن صفحهٔ اول ذخیره شده، اما صفحهٔ دوم پس از تمام retryها شکست می‌خورد:

```
page 1 → ذخیره شد
page 2 → شکست نهایی
```

راه‌حل:

- داده‌های صحیح صفحهٔ اول باقی بمانند.
- SyncRun ناموفق شود.
- watermark جلو نرود.
- اجرای بعدی دوباره از watermark قبلی شروع شود.
- Upsert مانع duplicate شدن صفحهٔ اول شود.

قاعدهٔ اصلی:

```
هر صفحه → داخل transaction دیتابیس
watermark → فقط بعد از موفقیت تمام صفحات
```

نباید بعد از دریافت هر صفحه watermark را تغییر دهیم.





## ۱۱. مبلغ‌های اعشاری

مبالغ به شکل string می‌آیند:

```
{
  "amount": "1204.50"
}
```

نباید به `float` تبدیل شوند:

```
# نامناسب
float("1204.50")
```

باید از `Decimal` استفاده شود:

```
from decimal import Decimal

amount = Decimal("1204.50")
```

و در دیتابیس:

```
models.DecimalField(
    max_digits=18,
    decimal_places=2,
)
```

 





## ۱۲. چند ارز مختلف

مبالغ `USD`، `EUR` و `GBP` را نمی‌توان مستقیم جمع کرد:

```
100 USD + 100 EUR + 100 GBP ≠ 300
```

باید بر اساس ارز جدا شوند:

```
{
  "USD": "100.00",
  "EUR": "100.00",
  "GBP": "100.00"
}
```

یا در آینده با نرخ تبدیل معتبر به یک ارز پایه تبدیل شوند؛ اما نرخ تبدیل در این پروژه داده نشده، پس جمع جداگانه انتخاب صحیح‌تری است.





## ۱۳. پاسخ ناقص یا نامعتبر

قبل از ذخیره باید بررسی کنیم پاسخ مورد انتظار را دارد:

```
items
page
page_size
total
next_page
```

و هر invoice یا transaction نیز فیلدهای ضروری را داشته باشد.

اگر پاسخ `200` باشد ولی JSON خراب یا ناقص باشد، نباید آن صفحه را خالی فرض کنیم؛ چون در این صورت داده از دست می‌رود. باید اجرای sync شکست بخورد و watermark ثابت بماند.

## 







































را درخواست ها رو همزمان ارسال نمیکنیم؟  چرا تا یه صفحه ای گرفته نشه اصلا صفحه بعدی رو نمیگیرم؟

Tuesday 9:50 PM

هم‌زمان نفرستادن درخواست‌ها یک الزام فنی مطلق نیست؛ یک انتخاب طراحی برای ساده‌تر و قابل‌اعتمادتر شدن sync است.

## دلیل اول: مسیر صفحه‌بندی با `next_page` مشخص می‌شود

پاسخ هر صفحه می‌گوید صفحهٔ بعدی چیست:

```
{
  "page": 1,
  "next_page": 2
}
```

و آخرین صفحه:

```
{
  "page": 3,
  "next_page": null
}
```

پس جریان طبیعی API این است:

```
صفحه ۱ را بگیر
→ next_page را بخوان
→ صفحه ۲ را بگیر
→ next_page را بخوان
→ ادامه بده
```

قبل از دریافت صفحهٔ اول، رسماً نمی‌دانیم چند صفحه وجود دارد.

می‌توان بعد از صفحهٔ اول از `total` تعداد صفحات را حساب کرد و بقیه را موازی گرفت، اما چون داده‌های vendor حین sync تغییر می‌کنند، `total` ممکن است ثابت نماند.

## دلیل دوم: مدیریت خطا ساده‌تر است

در حالت ترتیبی:

```
page 1 → موفق و ذخیره شد
page 2 → خطا و retry
page 3 → هنوز شروع نشده
```

دقیقاً می‌دانیم کجا هستیم.

در حالت موازی ممکن است:

```
page 2 → شکست
page 3 → موفق
page 4 → موفق
page 5 → هنوز در حال اجرا
```

حالا مدیریت وضعیت دشوارتر می‌شود:

- کدام صفحه باید retry شود؟
- چه زمانی resource کامل محسوب می‌شود؟
- اگر یکی شکست بخورد، درخواست‌های دیگر لغو شوند یا ادامه دهند؟
- چه زمانی watermark جلو برود؟
- شمارنده‌های SyncRun چگونه به‌روزرسانی شوند؟

این مشکلات حل‌شدنی‌اند، ولی برای این پروژه پیچیدگی بی‌دلیل ایجاد می‌کنند.





# مشکل watermark و قاعده درست

واترمارک یه تاریخ هست که میگه ما داده ها رو تا چه تاریخی دریافت کردیم و قبل از اون رو دیگه نیاز نیست دریافت کنیم! یعنی ما میخوایم incremental sync داشته باشیم و هر بار از اول نیایم همه داده ها رو بگیریم.

از اونجایی که سرویس خارجی داده هاش رو بصورت page , page size میده و cursor pagination نداره و همزمان داده هاش در حال تغییر هستن و یه سری رکورد اضافه و یه سری رکورد ابدیت میشن ما تا به انتهای تمام صفحات توی یک اجرای sync run نرسیدیم نباید مقدار water mark رو جلو ببریم(نباید بعد از ذخیره رکوردهای یک صفحه بروزبشه). چون ممکنه یه سری از رکورد ها رو هیچوقت نگیریم و جا بیفتن! در ادامه با مثال این موردو ببینیم:





## یک اجرای کامل Sync

فرض کنیم واترمارک ذخیره‌شده در دیتابیس این است:

```
watermark = 10:00
```

یعنی:

```
تمام تغییرات تا ساعت 10:00 قبلاً پردازش شده‌اند.
```

داده‌های vendor:

```
A → 10:01
B → 10:02
C → 10:03
D → 10:04
E → 10:05
F → 10:06
```

با `page_size=2`، درخواست Sync باید این باشد:

```
updated_since=10:00
```

صفحات این مجموعه:

```
صفحه 1: A, B
صفحه 2: C, D
صفحه 3: E, F
```

## روش اشتباه: واترمارک بعد از هر صفحه تغییر کند

### مرحلهٔ اول: دریافت صفحهٔ اول

درخواست:

```
GET /invoices?updated_since=10:00&page=1&page_size=2
```

پاسخ:

```
A → 10:01
B → 10:02
next_page = 2
```

ما `A` و `B` را ذخیره می‌کنیم و بلافاصله واترمارک دیتابیس را جلو می‌بریم:

```
watermark = 10:02
```

### مرحلهٔ دوم: دریافت صفحهٔ دوم

اگر درخواست بعدی را با واترمارک جدید ارسال کنیم:

```
GET /invoices?updated_since=10:02&page=2&page_size=2
```

دیگر مجموعهٔ قبلی را صفحه‌بندی نمی‌کنیم. به دلیل inclusive بودن فیلتر، مجموعهٔ جدید این است:

```
B → 10:02
C → 10:03
D → 10:04
E → 10:05
F → 10:06
```

صفحه‌بندی این مجموعه:

```
صفحه 1: B, C
صفحه 2: D, E
صفحه 3: F
```

چون ما `page=2` خواسته‌ایم، پاسخ می‌شود:

```
D, E
```

بنابراین `C` جا می‌افتد.

علت این است که هم‌زمان دو چیز را تغییر دادیم:

```
updated_since: از 10:00 به 10:02
page:          از 1 به 2
```

تغییر فیلتر باعث شد اعضای صفحات از نو چیده شوند. صفحهٔ دوم جدید، همان صفحهٔ دوم قبلی نیست.

### مرحلهٔ سوم

بعد از ذخیرهٔ `D` و `E`، واترمارک را دوباره تغییر می‌دهیم:

```
watermark = 10:05
```

سپس درخواست می‌زنیم:

```
GET /invoices?updated_since=10:05&page=3&page_size=2
```

مجموعهٔ جدید فقط این دو رکورد را دارد:

```
E → 10:05
F → 10:06
```

این مجموعه فقط یک صفحه دارد:

```
صفحه 1: E, F
```

ولی ما صفحهٔ سوم را درخواست کرده‌ایم؛ بنابراین پاسخ خالی می‌شود و ممکن است Sync را تمام‌شده تصور کنیم.

نتیجه:

```
ذخیره‌شده: A, B, D, E
جاافتاده:  C, F
```

## روش درست: واترمارک اجرای Sync ثابت باشد











#  چرا شماره صفحه رو ثابت نگه نداریم و واتر مارک رو بروز نکنیم؟

اینکار بخاطر inlcuse بودن فیلتر سمت سرویس خارجی ممکنه باعث بشه روی یه جا گیر کنه و هیچوقت جلو نره سینک داده ها. یعنی مثلا اندازه پیج ما فرضا ۱۰ هست و توی جواب برگشتی زمان اخرین آبدیت داده ها همون مقدار واتر مارک فعلی باشه! در نتیجه همینجا گیر میکنه تسک



در ادامه مثالشو ببینیم:



```
همیشه page=1
بعد از هر پاسخ، watermark را برابر آخرین updated_at می‌کنیم
```

این ایده ظاهراً شبیه cursor pagination عمل می‌کند و در بعضی شرایط کار می‌کند، اما به‌خاطر inclusive بودن فیلتر یک مشکل اساسی دارد.



## مشکل اصلی: timestampهای یکسان

 فرض کنیم سه رکورد زمان یکسانی دارند:

```
A → 10:01
B → 10:01
C → 10:01
D → 10:02
```

و `page_size=2` است.

درخواست اول:

```
GET ?updated_since=10:00&page=1&page_size=2
```

پاسخ:

```
A(10:01), B(10:01)
```

واترمارک را به `10:01` تغییر می‌دهیم.

دوباره صفحهٔ اول را می‌گیریم:

```
GET ?updated_since=10:01&page=1&page_size=2
```

به دلیل inclusive بودن، مجموعه همچنان این است:

```
A(10:01)
B(10:01)
C(10:01)
D(10:02)
```

صفحهٔ اول دوباره می‌شود:

```
A(10:01), B(10:01)
```

هرچقدر درخواست را تکرار کنیم، باز همین دو رکورد را می‌گیریم:

```
page=1, watermark=10:01 → A, B
page=1, watermark=10:01 → A, B
page=1, watermark=10:01 → A, B
...
```

پس:

- Sync در حلقه گیر می‌کند.
- `C` هیچ‌وقت دریافت نمی‌شود.
- به `D` هم نمی‌رسیم









## checkpoint و موفقیت منبع

بعد از رسیدن به `next_page=null`، این دو تغییر باید با هم و در یک transaction انجام شوند:

```
checkpoint همان منبع جلو برود
وضعیت همان منبع successful شود
```

مثلاً:

```
with transaction.atomic():
    cursor.checkpoint = scan_started_at
    cursor.save()

    resource_result.status = "succeeded"
    resource_result.save()
```

اگر قبل از این مرحله worker کرش کند، checkpoint قبلی باقی می‌ماند و صفحات دوباره خوانده می‌شوند؛ این تکرار بی‌خطر است.

اما هرگز نباید این ترتیب را داشته باشیم:

```
اول checkpoint جلو برود
بعد داده‌ها ذخیره شوند
```

چون اگر بین این دو مرحله crash شود، checkpoint از داده‌های واقعاً ذخیره‌شده جلوتر می‌افتد و ممکن است رکورد از دست برود.







یه نکته دیگه اینکه وقتی سینک شروع میشه بهتره کمی قبلتر از زمان checkpoint فعلی رو درخواست کنیم. چون ممکنه :

- ساعت سرور سرویس خارجی با سرویس ما کمی اختلاف داشته باشه
- چند تا تغییر توی یه ثانیه همشون انجام شده باشن
- همزمان که ما داریم سینک میکنیم همون لحظه همون رکورد عوض شده باشه
- یه تغییری سمت سرویس خارجی با کمی تاخیر ثبت شده باشه



# داشبورد نیمه بروز شده

چون بعد از دریافت هر رکوردی ما میایم و دیتابیس رو ابدیت میکنیم ممکنه ادمین موقتا توی گزارشش بخشی از داده ها براش قدیمی(تارکنش ها) و بخشی مثل بروز شده(فاکتورها) ببینه. هیچ جایی از تسک خواسته نشده که وضعیتی که ادمین میبینه باید کاملا بروز و consist باشه. خود ادمین هم میتونه ببینه یه سینکی در حال اجرا هست یا شکست خورده و آگاه میشه که داده ها کامل بروز نیستن





اگه بخواد ادمین داده سینک نیمه نصفه رو نبینه (چون ممکنه بخواد تصمیم مالی بگیره مثلا برای گزارش مالی رسمی یا دستور  پرداخت بده):



یه راه اینه یه جدولی داشته باشیم به اسم sync_state که کلا یه رکورد داره و توی اون رکورد شماره ورژن فعلی نوشته شده. بعد یه ستون توی جدول های invoice و factor هم داشته باشیم که روی هر رکورد ثبت میشه توی چه نسخه ای ایجاد یا ابدیت شده. در نتیجه وقتی ادمین کویری میزنه که اطلاعات رو ببینه توی کویری اخرین نسخه پایدار رو بهش نشون میدیم.



یا باید کل sync run رو توی یه تراکنش دیتابیسی انجام بدیم! که این یعنی تراکنش دیتابیس خیلی طولانیه که کانکشن دیتابیس رو طولانی نگه میداره و باعث table bloat توی پستگرس میشه 

## 





## cooldown مشترک بین endpointها

 سرویس خارجی یک request log مشترک برای هر دو endpoint دارد و `Retry-After` را از همان محدودیت مشترک تولید می‌کند؛ در [main.py (line 73)](/home/milad/fullstack-interview/mock-service/app/main.py:73) قابل مشاهده است.

فرض کن آخرین درخواست invoices پاسخ دهد:

```
429 Retry-After: 30
```

اگر بلافاصله به transactions درخواست بزنیم، احتمالاً آن هم `429` می‌گیرد.

بنابراین client باید یک مقدار مشترک داشته باشد:

```
blocked_until = now + 30 seconds
```

قبل از هر درخواست، چه invoices و چه transactions:

```
اگر اکنون < blocked_until:
    تا blocked_until منتظر بمان
```

اگر این انتظار از deadline کل عبور می‌کند، اجرا متوقف می‌شود.





## محدودیت اسکن مجدد و عدم پیشرفت

Retry درخواست با اسکن مجدد فرق دارد:

```
Retry درخواست:
همان page را به علت 503 دوباره بگیر

Rescan:
به علت مشکوک بودن pagination، دوباره از page=1 شروع کن
```

هر دو باید محدود باشند، مثلاً:

```
حداکثر پنج attempt برای هر درخواست
حداکثر سه pass برای هر منبع
```

همچنین اگر API رفتار غیرعادی داشت، مانند:

```
page=2 → next_page=2
```

یا مرتباً همان صفحه را برگرداند، باید آن را «عدم پیشرفت» تشخیص دهیم و متوقف شویم؛ وگرنه حلقه بی‌نهایت می‌شود.





## خطای مستقل منبع در برابر خطای زیرساخت

این بخش ایجنت نیز درست است.

اگر invoices خطای مستقل بگیرد:

```
Invoices endpoint → 503
Transactions endpoint → ممکن است سالم باشد
```

پس بعد از تمام شدن retryهای invoices، در صورت داشتن زمان و رعایت cooldown، می‌توانیم transactions را امتحان کنیم.

اما اگر مشکل مشترک و زیرساختی باشد:

```
دیتابیس قطع شده
قفل اجرای Sync از دست رفته
تنظیمات اصلی سرویس نامعتبر است
```

نباید ادامه بدهیم؛ چون دیگر نمی‌توانیم ذخیره‌سازی امن یا جلوگیری از اجرای هم‌زمان را تضمین کنیم.

## 





## بازیابی Sync پس از Crash و مدیریت `SyncRun`

برای جلوگیری از اجرای هم‌زمان دو Sync، هر task قبل از شروع کار تلاش می‌کند advisory lock بگیرد.

اگر نتواند قفل را بگیرد، یعنی Sync دیگری در حال اجراست؛ بنابراین اجرای جدید بدون تغییر دادن `SyncRun` قبلی، با وضعیت `skipped` تمام می‌شود.

اگر قفل را بگیرد، رکوردهای `SyncRun` با وضعیت `running` بررسی می‌شوند:

- اگر هیچ اجرای `running` وجود نداشته باشد، یک `SyncRun` جدید برای task فعلی ساخته می‌شود.
- اگر `task_id` اجرای `running` با `task_id` فعلی یکسان باشد، یعنی همان پیام Celery دوباره تحویل داده شده است؛ بنابراین همان `SyncRun` دوباره استفاده می‌شود.
- اگر `task_id`ها متفاوت باشند، یعنی اجرای قبلی بدون ثبت نتیجه متوقف شده است؛ اجرای قبلی `interrupted` می‌شود و یک `SyncRun` جدید برای task فعلی ساخته می‌شود.
- اگر همین `task_id` قبلاً با موفقیت تمام شده باشد، اجرای مجدد به‌صورت no-op خاتمه پیدا می‌کند.

استفادهٔ دوباره از یک `SyncRun` به معنی ادامه از خط یا صفحهٔ زمان crash نیست. task از ابتدای تابع اجرا می‌شود و با استفاده از checkpointهای ذخیره‌شده ادامه می‌دهد. ممکن است بعضی صفحات دوباره دریافت شوند، اما Upsert و idempotent بودن Sync از ایجاد دادهٔ تکراری جلوگیری می‌کند.

### رفتار با تنظیمات پیش‌فرض Celery

در حالت پیش‌فرض، Celery پیام را قبل از اجرای task تأیید می‌کند. اگر worker وسط اجرا crash کند، آن پیام معمولاً دوباره توسط RabbitMQ تحویل داده نمی‌شود.

در این حالت:

```
Task A شروع می‌شود
SyncRun A = running
Task A crash می‌کند
قفل آزاد می‌شود
SyncRun A همچنان running می‌ماند

Task B در اجرای دستی یا زمان‌بندی بعدی شروع می‌شود
Task B قفل را می‌گیرد
SyncRun A را interrupted می‌کند
SyncRun B را می‌سازد
Sync از checkpoint قبلی شروع می‌شود
```

بنابراین بازیابی در اجرای دستی یا زمان‌بندی‌شدهٔ بعدی انجام می‌شود.

### رفتار با `acks_late` و `reject_on_worker_lost`

اگر بعداً این تنظیمات فعال شوند:

```
acks_late=True
reject_on_worker_lost=True
```

پیام تا پایان موفق task تأیید نمی‌شود. در صورت crash شدن worker، RabbitMQ می‌تواند همان پیام را با همان `task_id` دوباره تحویل دهد.

در این حالت:

```
Task A با task_id مشخص شروع می‌شود
SyncRun A = running
Task A crash می‌کند
قفل آزاد می‌شود

همان Task A دوباره تحویل داده می‌شود
Task A قفل را می‌گیرد
SyncRun دارای همان task_id را پیدا می‌کند
همان SyncRun را دوباره استفاده می‌کند
Sync از checkpoint قبلی شروع می‌شود
```

این طراحی با هر دو سیاست acknowledgment سازگار است:

- در حالت پیش‌فرض، اجرای بعدی با ID جدید، اجرای قبلی را `interrupted` می‌کند.
- در حالت redelivery، همان task ID به همان `SyncRun` متصل می‌شود.
- در هر دو حالت، task از checkpoint امن شروع می‌شود و اجرای تکراری با Upsert قابل‌تحمل است.

تغییر وضعیت اجرای قبلی و ساخت یا بازیابی اجرای فعلی باید پس از گرفتن advisory lock و داخل یک transaction کوتاه دیتابیس انجام شود.









پاسخ HTTP موفق، الزاماً دادهٔ قابل ذخیره نیست**

سیاست برخورد با دادهٔ نامعتبر:

- JSON خراب یا envelope ناقص؛
- شناسه یا timestamp ضروریِ ناموجود؛
- مبلغ نامعتبر، نامتناهی یا خارج از ظرفیت `DecimalField`؛
- `next_page` تکراری یا ناسازگار؛
- مقدار جدید برای status، type یا currency.

برای این پروژه،بهتره که صفحهٔ دارای دادهٔ ضروری نامعتبر ذخیره نشود، خطا با شناسهٔ رکورد ثبت شود و checkpoint آن منبع جلو نرود. رد کردن بی‌صدای رکورد و جلو بردن watermark، داده را پنهانی گم می‌کند.

پول باید مستقیم از رشته به `Decimal` تبدیل شود و ارز هر invoice و transaction حفظ شود.

## 